names <- c('Adi', "Budi", 'Cici', "Dedi")

m = matrix(1:12,nrow=3)
a = array(1:24, c(3,2,4))



a = array(1:100000, c(2,50,100))

users <- data.frame(
  name = names,
  gender = c('male', 'female', 'male', 'female'),
  age = c(15, 20, 25, 30)

l = list(a,names,users)
